# Module 012 — File Watcher

**implementation date:** 2026-07-08  
**review date:** 2026-07-08  
**reviewer:** Architecture review (Grok-assisted) + independent reviews  
**architecture score:** 10/10  
**implementation_score:** 10/10  
**status:** merged  
**merge_date:** 2026-07-08  
**commit:** 20673bffaeb2984a16bc08f578eac231b949fabe  
**merge tag:** core-platform-001-012-complete  

## Highlights (from review + validation)
- Core responsibility: Detect filesystem changes only on 011-validated roots. Publish canonical events (`fs:change:created|modified|deleted|renamed@1.0` + overflow).
- Layering preserved: 011 File System Service validates → 012 File Watcher observes validated roots and emits events → 013 IPC Framework will own transport.
- Module 012 owns: watcher lifecycle (idle/watching/disposed), subscriptions, normalized event emission, resource cleanup (dispose + closeAllWatches).
- Does **NOT** own: path validation (011), parsing/indexing/graph (higher), persistence (016), transport/IPC (013), project/workspace lifecycle.
- Ponytail compliance: native `node:fs.watch` + manual recursive attach + simple debounce/rename heuristic. `ponytail:` comments with upgrade path (chokidar later). No new dependencies.
- Structured logs: `[module=file-watcher] phase=...` with sanitized paths.
- Tests: 4 dedicated FTs in core (idle state, create/modify/delete events on real tmp FS, update watches, dispose lifecycle); 17/17 total.
- Consumer: Thin `useFileWatcher.ts` (getFileWatcher, initFileWatcher, updateWatcherRoots, subscribeToFsChanges, dispose); minimal demo commands in App.tsx (ponytail until 013).
- Security: Only watches caller-provided validated roots. Bounded readdir/attach under roots. Explicit cleanup. No raw content ops or validation duplication.
- Events are transport-independent (stdlib listeners). Ready for 013.

## Constitution Compliance
All ✅. No violations. Matches 009/010/011 pattern exactly. Constitution priorities (Security first), ponytail ladder, monorepo boundaries, dep direction (011 → 012), module ownership, no drift.

## Specific Pre-Merge Checks
**Validated roots only: CONFIRMED.**
- setWatches accepts roots exclusively from 011-validated flow (demo: updateFsRoots + initFileSystem then initFileWatcher).
- No internal validation or scope checks performed by 012 (by design per spec/manifest).
- All emitted events carry paths under watched validated roots.

**No ownership of future modules:**
- No IPC / transport implementation (reserved for 013).
- No indexing, parsing, graph, SQLite.
- No content FS access (read/write/list) — only platform watch primitives for detection.

**Lifecycle & resource management:**
- Explicit states, setWatches replacement closes prior watches.
- subscribe returns unsubscribe fn.
- dispose clears everything.
- Best-effort rename correlation (platform limits acknowledged).

## Minor Concerns (acknowledged, intentionally deferred per feedback)
- Native fs.watch rename info is limited on Windows (delete+create sequences common); heuristic is minimal and documented.
- oldPath not always populated (consumers tolerate delete+create per spec).
- Demo commands remain in App.tsx (extraction later when volume justifies).
- Logging uses `console.warn`; full Logger deferred.
- Test FTs use direct node fs on tmp (acceptable isolation; real flows go through 011 init first).

## Follow-up issues
- None blocking.
- All per spec (012-file-watcher.md), validation spec, merge readiness, and independent reviews (10/10).
- Native watcher sufficient for current scope; chokidar upgrade path noted.

## Notes
See project-status.json mergeMetadata.012, 000-current-status.md, handoff.md.
Implementation + Architecture 10/10. Zero drift. Process (Spec ↓ Impl ↓ Validation ↓ Arch Review ↓ Merge Readiness ↓ Before Merge Fallback ↓ Independent Merge Review → --ff-only Merge) followed without skipping gates.
Ready for Module 013 – IPC Framework after this merge. Workflow praised as stable and repeatable.

**Merge strategy used:** `git checkout main; git merge --ff-only before-012-merge` (linear history preserved). Tag: core-platform-001-012-complete.