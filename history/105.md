# Module 105 — Dependency Graph (History)

**Module:** 105 Dependency Graph  
**Phase:** Phase B.2 — D1 third target (after 103, 104)  
**Status:** ✅ Published / Frozen  
**Date:** 2026-07-11  

## Summary

Dependency graph B.2 slice under `@ray-studio/ingestion` (`packages/ingestion/src/dependency/**`). Consumes frozen Modules **103** and **104** (parser + extractor) only — no rewrite of frozen modules. In-process edges via `computeDelta` + apply/query; **no** Memory Engine **201**. Relationship types required for B.2: `dependsOn`, `calls`. Edge keys: `type|from|to`; `fileId` = `file:<normalized path>`. Languages: typescript, tsx, javascript, jsx, python. No IPC, no `@ray-studio/core`, no 102 indexer ownership.

## Scores

| Review | Score |
|--------|-------|
| Architecture | PASS WITH MINOR COMMENTS |
| Layer 4 validation | PASS (FT-001…010 + edges + perf; ingestion **45/45** = 16+15+14) |
| Merge Readiness | APPROVE WITH MINOR COMMENTS |
| Independent Merge Review | APPROVE WITH MINOR COMMENTS |
| Before Merge Fallback | PASS (`before-105-merge` retained) |

## Accepted architectural debt (non-blocking)

| ID | Debt | Status |
|----|------|--------|
| **R1** | Coarse multi-file `keysByFile` ownership in `memory-store.ts` | **Accepted for Phase B.2** — does not block freeze; upgrade before true single-file incremental / **201** |

Other minor (non-blocking): no `EdgeStore` port yet; in-process instance scope.

## Commits / Tags

| Role | Hash / Name |
|------|-------------|
| Feature (published; tag target) | `8ede948` |
| Finalize (status / history freeze) | *(this finalize commit — pin after)* |
| Rollback branch | `before-105-merge` @ `7f8337c` |
| Checkpoint tag | `phase-b2-105-complete` @ `8ede948` |
| Prior checkpoint | `phase-b2-104-complete` @ `f68a106` |
| Package | `@ray-studio/ingestion` |

## Artifacts

- Spec: `prompts/modules/105-dependency-graph.md` (Draft — B.2 freeze via Layer 4)
- Layer 4: `prompts/validation/105-dependency-graph.validation.md`
- Manifest: `implementation-manifests/105-dependency-graph.json`
- Code: `packages/ingestion/src/dependency/**` + package export wiring

## Tests

- Ingestion: **45/45** (16×103 parser + 15×104 extractor + 14×105 dependency)
- Core regression: **48/48** (untouched)
- Typecheck: `tsc` PASS for `@ray-studio/ingestion`

## Scope (frozen)

- Domain: `packages/ingestion/src/dependency/**`
- Allowed wiring: `packages/ingestion/src/index.ts`, `packages/ingestion/package.json`
- Hard dependencies: Modules **103** + **104** only (consume)
- Public API: `createDependencyGraph`, `computeDelta`, apply/query helpers, relationship types, `edgeKey` / `fileId` / path normalize

## Deferred (not 105 defects)

- Module **102** Incremental Indexer — **next planning label only**
- 101 live adapters / SymbolContext mapping
- Graph upsert / Memory Engine (**201**)
- Per-file edge ownership upgrade (**R1**)
- Optional `EdgeStore` port when 201 lands
- IPC surface for graph queries

## Immutable (105)

Except defect fixes and security. Do not expand 105 into Memory Engine ownership, indexer ownership, IPC, or Core Platform edits without a new module authorization.

## Follow-ups

- **Do not start Module 102, 201, or 301** without explicit owner authorization.
- `nextModule: "102"` is a **planning label only** (D1 order after 105-slice).
- Prefer feature branch for 102+ when authorized.
