# Module 104 — Symbol Extractor (History)

**Module:** 104 Symbol Extractor  
**Phase:** Phase B.2 — D1 second target (after 103)  
**Status:** ✅ Published / Frozen  
**Date:** 2026-07-11  

## Summary

Symbol extraction under `@ray-studio/ingestion` (`packages/ingestion/src/extractor/**`). Consumes frozen Module 103 `SyntaxTree` / `SyntaxNode` (no parser rewrite). Ingestion-local `Symbol` model (no `@ray-studio/core` import). Walker-primary extraction with language `.scm` query assets (validated/load-cached). Supports typescript, tsx, javascript, jsx, python. Nested qualified names, JSDoc / Python docstrings, partial-parse tolerance, determinism, `extractWithMetrics`. No IPC, no graph storage, no 102/105/201.

## Scores

| Review | Score |
|--------|-------|
| Architecture | PASS |
| Layer 4 validation | PASS (FT-001…FT-010; 31/31 ingestion) |
| Merge Readiness | APPROVED |
| Independent Merge Review | APPROVE WITH MINOR COMMENTS |

## Commits / Tags

| Role | Hash / Name |
|------|-------------|
| Feature (published; tag target) | `f68a106` |
| Finalize (status / history freeze) | `087efbb` |
| Hash pin | (pin commit if tip advances) |
| Rollback branch | `before-104-merge` @ `4d55c4c` |
| Checkpoint tag | `phase-b2-104-complete` @ `f68a106` |
| Prior checkpoint | `phase-b2-103-complete` @ `35396af` |
| Package | `@ray-studio/ingestion` |

## Artifacts

- Spec: `prompts/modules/104-symbol-extractor.md` (Draft — B.2 freeze via Layer 4)
- Layer 4: `prompts/validation/104-symbol-extractor.validation.md`
- Manifest: `implementation-manifests/104-symbol-extractor.json`
- Code: `packages/ingestion/src/extractor/**` + package export wiring

## Tests

- Ingestion: **31/31** (16×103 parser + 15×104 extractor)
- Core regression: **48/48** (untouched)

## Scope (frozen)

- Domain: `packages/ingestion/src/extractor/**`
- Allowed wiring: `packages/ingestion/src/index.ts`, `packages/ingestion/package.json` (`.scm` copy on build)
- Hard dependency: Module **103** only
- Public API: `createSymbolExtractor`, `extract` / `extractWithMetrics`, `Symbol` / `SymbolKind`, query language helpers

## Deferred (not 104 defects)

- Module 105 Dependency Graph (B.2 slice) — **next planning target**
- Module 102 Incremental Indexer
- 101 live adapters / SymbolContext mapping
- Graph upsert / Memory Engine (201)
- Deep `Object.freeze` on Symbol outputs
- Query-mode extraction (walker is primary; `.scm` assets retained)

## Immutable (104)

Except defect fixes and security. Do not expand 104 into graph ownership, indexer, IPC, or Core Platform edits without a new module authorization.

## Follow-ups

- **Do not start Module 105 governance or implementation** without explicit owner authorization.
- `nextModule: "105"` is a planning label only (D1 order: 105-slice).
- Prefer feature branch for 105+ when authorized.
