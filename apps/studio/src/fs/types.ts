/**
 * 011 File System Service — Studio re-exports (thin)
 */
export type {
  ValidatedPath,
  FileMetadata,
  ReadFileResult,
  WriteFileResult,
  ListDirectoryResult,
  FsOperationError,
  FsValidationResult,
  FsServiceState,
} from '@ray-studio/core';
