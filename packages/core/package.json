{
  "name": "@ray-studio/core",
  "version": "0.1.0",
  "private": true,
  "description": "Ray Studio shared core domain (workspace, project, fs, etc.)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "eslint": "^9.25.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.8.0",
    "vitest": "^1.6.0"
  }
}
